import React from 'react'

const ActivityCard = ({ user, userProfile, handleAddActivity, handleRemoveActivity, handleUpdateActivity }) => {
  console.log('user', user)
  console.log('userProfile', userProfile)
  return (
    <>
      <h1>Sanity Check!</h1>
    </>
  );
}
 
export default ActivityCard;

